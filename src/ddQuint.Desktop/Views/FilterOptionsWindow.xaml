<Window x:Class="ddQuint.Desktop.Views.FilterOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Filter Options"
        Height="180" Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource WindowBackgroundBrush}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Filter Options" 
                   FontSize="16" FontWeight="SemiBold"
                   Foreground="{StaticResource TextBrush}"
                   Margin="0,0,0,15"/>
        
        <!-- Filter options -->
        <CheckBox Grid.Row="1" x:Name="HideBufferZoneCheckBox"
                  Content="Hide Buffer Zone Samples"
                  IsChecked="{Binding HideBufferZone}"
                  Foreground="{StaticResource TextBrush}"
                  Margin="0,0,0,10"/>
        
        <CheckBox Grid.Row="2" x:Name="HideWarningsCheckBox"
                  Content="Hide Warning Samples"
                  IsChecked="{Binding HideWarnings}"
                  Foreground="{StaticResource TextBrush}"
                  Margin="0,0,0,15"/>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" 
                   HorizontalAlignment="Right">
            <Button Content="Cancel" 
                   Click="Cancel_Click"
                   Style="{StaticResource MacOSButton}"
                   Width="80" Margin="0,0,10,0"/>
            <Button Content="Apply" 
                   Click="Apply_Click"
                   Style="{StaticResource MacOSButton}"
                   Width="80"/>
        </StackPanel>
    </Grid>
</Window>